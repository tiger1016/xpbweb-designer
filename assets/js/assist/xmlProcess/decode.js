const decodePanel = (layer, xmlDom) => {
  
}

const decodeRect = (layer, xmlDom) => {
  
}

const decodeEllipse = (layer, xmlDom) => {
  
}

const decodePicture = (layer, xmlDom) => {
  
}

const decodeBarcode = (layer, xmlDom) => {
  
}

const decodeLine = (layer, xmlDom) => {
  // console.log(xmlDom)
  // const actLayer = xmlDom.getElementsByTagName('ActiveLayer');
  
}

const decodeText = (layer, xmlDom) => {
  
}